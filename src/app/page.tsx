import { ChatInterface } from "@/components/chat/chat-interface";

const page = () => {
  return (
    <main className="min-h-screen bg-main">
      <ChatInterface />
    </main>
  );
};

export default page;