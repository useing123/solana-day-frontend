/** @type {import('next').NextConfig} */
const nextConfig = {
  outputFileTracingRoot: '/home/useing123/Desktop/job_application/solana-day-frontend',
  eslint: {
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
